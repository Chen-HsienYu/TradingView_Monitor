# Old YAML config (no longer used)
config.yaml

# Python
__pycache__/
*.pyc

# Environment
.env

# Note: users/users.csv is NOT ignored - it needs to be deployed
# Passwords are hashed with bcrypt so safe to commit
